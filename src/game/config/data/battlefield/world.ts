import worldJson from './world.json';
import { BattlefieldWorldConfig } from '../../../../types/game.types';

export const battlefieldWorld: BattlefieldWorldConfig = worldJson as BattlefieldWorldConfig;
