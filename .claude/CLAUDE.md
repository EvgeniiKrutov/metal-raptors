## Style rules
- Never use comments directly in the code
- If it is needed, all documentation should be updated through .md files under /docs folder
## Build rules
- Do not ask permission for running tsc, eslint npm run build commands, all these commands are allowed to be executed by default
- Do not spend token on using Playwright, fix and implement issues without that tool